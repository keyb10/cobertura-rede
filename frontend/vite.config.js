import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/cobertura-rede/', // IMPORTANT: This must match your GitHub repository name
})
